# theory/ 만 돌릴 때는 이 둘이면 된다. GPU 도 필요 없다.
numpy>=1.24
scipy>=1.11

# experiments/ 는 검출기를 돌린다. exp00/exp01/exp04 에 필요하다.
#   ultralytics 가 torch 와 opencv-python 을 끌고 오지만, 우리 코드가 직접
#   import 하므로(run_sequence.py 의 cv2, patched_nms 의 torch) 명시해 둔다.
ultralytics>=8.4
torch>=2.0
opencv-python>=4.8

# exp01/verify_data.py 의 MOT17 미러 대조에만 쓴다.
huggingface_hub>=0.20

# 여기 없는 것: exp02/exp03 은 콜랩에서 UTrack 클론 안의 의존성으로 돌린다
# (motmetrics, pycocotools, omegaconf, lap, nms_var CUDA 확장 등).
# 그쪽은 experiments/exp02_utrack_replication/colab_setup.md 를 따른다.
