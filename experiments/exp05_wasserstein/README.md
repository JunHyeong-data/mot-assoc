# 실험 5 — 와서스타인 통로에 DFL σ 를 넣는다 (**사전 선언**)

이론(`theory/divergence_channels.py`)이 **와서스타인만** 세 진단을 통과한다고 했고,
실험 1g 가 **DFL 이 크기모형을 이기는 유일한 소스**라고 했다. 처음으로
**소스와 통로가 둘 다 후보를 통과한 상태**다. 이제 HOTA 로 직접 잰다.

## 왜 이게 결정적인가

지금까지 우리 판정은 전부 **우도**였다 — 대리지표다. 실험 3 이 보였듯
**기하 통로는 σ 를 벌한다** (−3.81 HOTA). 우도에서 −0.1 nats 이긴다고
트래킹이 좋아진다는 보장이 전혀 없다. **이 실험이 그 질문에 직접 답한다.**

## 설계 — 검출을 한 번만 돌리고 트래커만 재생한다

갈래마다 검출기를 다시 돌리면 검출이 미세하게 달라질 여지가 생긴다.
**한 번 캐시하고 트래커만 재생하면 갈래 사이에서 검출이 비트 단위로 같다.**
갈리는 것이 비용함수 하나뿐이 된다. (`cache_detections.py`, exp00 `sweep.py` 와 같은 수법)

검출기는 **저자 가중치 + 포크 설정** — exp02·exp03 의 기준선(HOTA 64.494)과
같아야 숫자가 이어진다.

## 겨루는 갈래

`W²(t,d) = ‖ε‖² + tr(Σt + Σd − 2(Σt^½ Σd Σt^½)^½)` 를 비용으로 쓴다.
IoU 를 대체하는 것이 아니라 **같은 자리에 다른 거리를 넣는다.**

| 갈래 | Σ_d | 무엇을 묻나 |
|---|---|---|
| **A** baseline | — (맨 IoU) | 기준선. exp02 의 64.494 를 재현해야 한다 (**훅 검증**) |
| **W-DFL** | **DFL 분포 분산** | 이 실험의 주장 |
| **W-size** | `c·h²·I` (상자 크기) | **넘어야 할 것 — 이게 NWD 다** |
| **W-NMS** | NMS 후보 분산 | 소스×통로 격자를 채운다 (진단용) |

**`W-size` 가 결정적 통제군이다.** 실험 1e·1f 의 `C` 와 같은 것이고,
문헌에서 **NWD/LQTTrack 이 실제로 배포한 방법**이다. 허수아비가 아니다.

## 사전 선언한 판정

| # | 종말점 | 기준 |
|---|---|---|
| **[0]** | A(맨 IoU)가 **우리 하네스 안에서** 안정적인 기준선인가 | 아래 정정 참고 |
| **[0b]** | 기준선에서 트랙 간 `CV(Σt)` (수학 감사 (나)) | **CV < 0.05 면 검정력 없음.** 널을 증거로 쓰지 않는다 |
| **[1]** | **W-DFL − W-size** (HOTA) | **> 0 이면 DFL 소스가 통로에서도 낫다** |
| **[2]** | W-DFL − A (HOTA) | 와서스타인 통로 자체가 IoU 를 이기는가 |

> ### ⚠ 관문 [0] 을 고친다 (**결과 보기 전**)
>
> 처음에 "A 가 exp02 기준선 64.494 를 ±0.1 로 재현해야 한다" 고 적었다.
> **그건 성립할 수 없다.** exp02·exp03 의 64.494 는 **UTrack 의 `botsort`**
> (fast-GMC 포함)로 난 값이고, 여기서는 **ultralytics `BYTETracker`** 를 쓴다.
> 트래커 구현이 다르므로 절대값이 같을 이유가 없다.
>
> **고친 [0]**: 절대값을 맞추는 대신 **내적 타당성**을 본다.
> - 갈래 넷이 **같은 캐시된 검출**을 쓰고 **비용함수만** 다르다 (검출은 비트 단위 동일)
> - A 의 HOTA 를 그대로 보고하고, **exp02 와 다른 이유(트래커 구현)를 명시**한다
> - 판정 [1] 은 **갈래 간 차이**이므로 절대값이 달라도 유효하다
>
> **잃는 것**: exp02·exp03 과 숫자를 직접 이어 붙일 수 없다. 논문에서는
> "다른 하네스에서의 상대 비교" 로 적어야 한다. **이걸 결과 낸 뒤에 깨달았으면
> 사후 변명이 됐을 것이다.**

**[1] 이 주 종말점이다.** [2] 는 통로 효과이고 [1] 이 소스 효과다.
실험 1g 가 우도에서 본 것이 HOTA 로 옮겨가는지가 [1] 하나에 걸린다.

**판정폭**: exp03 이 −3.81 HOTA 를 "판정폭 0.3 의 13배" 라고 불렀다.
같은 자를 쓴다 — **|ΔHOTA| < 0.3 이면 "차이 없음"** 으로 읽는다.

## 미리 못박는 함정 다섯

1. **Σ 규모를 맞춰야 한다.** DFL σ 는 0.26배 과대, NMS σ 는 80배 과소다
   (실험 1b·1g). 날것으로 넣으면 **갈래 간 차이가 정보가 아니라 규모**가 된다.
   → **각 갈래의 Σ 를 `E[tr Σ]` 가 같아지도록 전역 배율로 맞춘다.**
   exp03 이 확장량을 맞춘 것과 같은 이유다
2. **트랙 쪽 Σt 를 무엇으로 둘 것인가.** 칼만 P 를 쓰면 갈래마다 다른 궤적을
   타서 통제가 깨진다. → **Σt 는 갈래와 무관하게 칼만 P 의 위치 성분으로 고정**
3. **W² 는 IoU 와 눈금이 다르다.** `match_thresh` 를 그대로 쓰면 안 된다.
   → 각 갈래에서 **채택률(매칭 수 / 최적쌍 수)을 A 와 맞추도록** 임계값을 보정하고,
   **보정 전후를 모두 보고**한다. 안 맞추면 통로 효과와 임계값 효과가 섞인다
4. **exp03 의 교훈**: 짝 일관성. 트랙이 검출의 Σ 를 물려받으면 정답쌍이 같이
   움직인다. → Σt 를 칼만 P 로 고정하면(함정 2) 이 문제가 원천 차단된다
5. **1회 실행이다.** 시드 변동폭을 모른다. 판정폭 0.3 미만은 주장하지 않는다

## 이 실험이 답하지 않는 것

- **DanceTrack·MOT20 에서도 같은가** — MOT17 val_half 7시퀀스뿐이다
- **[1] 이 양수여도 p 는 없다** — 시퀀스 7개짜리 HOTA 차이다. 실험 1g 의
  표본 문제(독립 장면 7개)가 여기도 그대로 있다

---

## 수학 — 대각 공분산에서 Bures 항이 떨어진다

우리 경우 Σ 가 전부 대각이다 (DFL 은 변마다 독립 softmax, 상자크기 모형은
정의상 대각, 칼만 P 의 위치 성분도 대각). 대각이면 행렬제곱근이 원소별이 되어

```
tr(Σt + Σd − 2(Σt^½ Σd Σt^½)^½)  =  Σ_i ( √σt_i − √σd_i )²
```

**표준편차 차이의 제곱합**으로 떨어진다. 싸고 정확하다.
`selftest.py [1]` 이 일반 행렬형과 대조해 **max|diff| = 2.1e-14** 를 확인했다.

## 트래커에 넣는 형태 — NWD 의 사상을 쓴다

`W²` 은 px² 이고 무한대까지 가는데 ByteTrack 비용은 [0,1] 이다. NWD 가 쓰는
지수 사상을 그대로 쓴다: `cost = 1 − exp(−√(W²)/C)`.
`C` 는 **채택률을 기준선과 맞추도록 보정**한다 (함정 3). 닫힌해가 있다.

---

## 수학 감사 — **결과를 보기 전에** 따진 것 (2026-08-17)

HOTA 를 하나도 계산하기 전에 비용함수의 수학을 감사했다. **셋이 나왔고
하나는 설계를 바꾼다.** 사전 선언을 그에 맞춰 고친다 (자료를 보기 전이므로 정당하다).

### (가) 할당에 도달하는 것은 **교차항 하나뿐**이다

```
Bures = Σᵢ(√σtᵢ − √σdᵢ)²  =  tr(Σt)  +  tr(Σd)  −  2Σᵢ√(σtᵢ·σdᵢ)
                              └행상수    └열상수    └── 유일한 비분리항
```

이중중심화 뒤 남는 것은 `−2Σᵢ√(σtᵢσdᵢ)` 하나다. 나머지는 할당이 안 본다.

### (나) **그 항은 Σt 가 트랙마다 달라야 산다** ← 설계 변경

`σt` 가 트랙 간 상수면 `−2√(σt)·√(σdⱼ)` 가 **순수 열함수**가 되어 사라진다.
합성으로 확인:

| Σt 의 트랙 간 변동 (CV) | \|R(Bures)\| |
|---|---|
| **0.00** | **0.0000** |
| 0.05 | 0.153 |
| 0.20 | 0.624 |
| 0.50 | 1.686 |
| 1.00 | 3.030 |

> **CV=0 이면 Σ_d 채널이 완전히 죽는다.** 즉 이 실험이 무언가를 찾을 수 있는지는
> **칼만 P 가 트랙마다 얼마나 다른가**에 달렸다. 실제로 P 는 상자 크기에 따라
> 변하므로 0 은 아니겠지만, **작으면 이 실험은 애초에 검정력이 없다.**

**따라서 판정 전에 관문을 하나 더 둔다:**

> **[0b] 기준선 실행에서 트랙 간 `CV(Σt)` 를 측정한다.**
> **CV < 0.05 이면 이 설계는 검정력이 없다.** 그때는 [1] 의 결과가 0 으로 나와도
> **"차이 없음"이 아니라 "못 잰다"로 보고한다.** 널 결과를 증거로 쓰지 않는다.

이건 exp03 의 사전 검정력 확인(`power_check.py`)과 같은 역할이다 — **비싼 실행이
원리적으로 무언가를 구별할 수 있는지 먼저 본다.**

### (다) 와서스타인은 **게이트를 넓히지 않는다**

`W² = ‖ε‖² + Bures`. 위치항에 `Σ⁻¹` 이 없다.

| Σt 배율 | 마할라노비스 d² | 와서스타인 위치항 |
|---|---|---|
| ×1 | 69.4 | 625.0 |
| ×64 | **1.1** | **625.0** |

마할라노비스는 64배 불확실해지면 위치 대가가 64배 싸진다(게이트가 열린다).
**와서스타인 위치항은 꿈쩍도 안 한다.**

> **와서스타인은 공분산 역설을 "메커니즘을 없애서" 푼다.**
> 마할라노비스는 과하게 보정하고, 와서스타인은 아예 보정하지 않는다.

### (라) 그래서 Σ_d 의 역할은 게이팅이 아니라 **분포 유사도**다

`Bures` 는 `σd = σt` 에서 **최소 0**이다. 즉 "나와 비슷한 불확실성을 가진 검출"을
선호한다. 이건 "불확실하니 관대해져라"가 아니라 **"불확실성 수준이 맞는 짝을
찾아라"** 다. **다른 종류의 정보다.**

> ### 이것이 실험 5 의 주장을 다시 규정한다
>
> 우리는 **"불확실성 인지 게이팅"을 만드는 게 아니다.**
> **"불확실성을 매칭 특징으로 쓰는 것"**을 시험하는 것이다.
>
> 그리고 이 구분이 논문에 좋다 — **두 의미론이 exp03 과 exp05 로 소진된다:**
> - **게이팅 의미론** (σ 로 상자를 키운다) → exp03, **−3.81 HOTA 로 실패**
> - **유사도 의미론** (σ 를 거리에 넣는다) → exp05, 지금 시험 중
>
> 어느 쪽이 나오든 "검출 불확실성을 연관에 쓰는 두 가지 방식"에 대한
> 통제된 답이 된다.

---

## 함정 3 을 내가 반만 지켰다 — 정정과 사후 검증 (2026-08-17)

사전 선언은 **"채택률(매칭 수 / 최적쌍 수)을 A 와 맞추도록 임계값을 보정"**
이라고 적었다. 그런데 구현(`wcost.solve_C`)이 실제로 맞추는 것은 **비용 중앙값**이다.
**같은 게 아니다.** 중앙값이 같아도 비용 분포의 모양이 다르면 `match_thresh=0.8`
아래로 들어오는 쌍의 비율이 달라진다. 그러면 갈래 간 HOTA 차이가
**통로 효과가 아니라 임계값 효과**가 된다.

**고치는 방법은 둘이었다:**
1. `solve_C` 를 채택률 이분법으로 다시 짠다
2. 보정은 중앙값으로 두고, **채택률이 결과적으로 맞았는지를 직접 잰다**

**2번을 택했다.** 1번은 결과를 본 뒤에 보정 절차를 바꾸는 것이라
사후 조정으로 읽힐 여지가 있다. 2번은 선언한 양(채택률)을 **측정해서
조건이 충족됐는지 확인**하는 것이라 절차를 건드리지 않는다.
**충족 안 됐으면 결과를 임계값 효과로 읽어야 한다** — 그 위험을 그대로 안는다.

`accept_rate.py` 가 잰다. **1단계 연관만** 센다 (`byte_tracker.py:326`,
`thresh=match_thresh`) — 개입 지점과 판정 지점을 일치시킨다. 2단계는
`get_dists` 를 안 쓰고, 3단계(unconfirmed, `:362`)는 임계값이 0.7 로 다르다.

```
채택률 = (1단계 매칭 수) / Σ_frames min(M, N)
```

**결과는 아래 「결과」 절에.**

---

## 결과 (2026-08-17, 7시퀀스 val_half)

### 관문 [0b] — 통과

트랙 간 `CV(Σt)` 중앙값 = **1.52** (w_dfl), **1.48** (w_size). 임계 0.05 의 **30배**.
수학 감사 (나)가 요구한 조건이 넉넉히 성립한다 — **이 설계는 검정력이 있다.**
따라서 아래 널 결과를 "못 잰다" 가 아니라 **"차이가 없다"** 로 읽어도 된다.

### 함정 3 — 채택률

| 갈래 | C | 1단계 매칭 | 최적쌍 상한 | 채택률 |
|---|---|---|---|---|
| A (IoU) | — | 47194 | 48993 | **96.3%** |
| W-DFL | 595.1 | 48408 | 48809 | **99.2%** |
| W-size | 597.3 | 48430 | 48810 | **99.2%** |

**2.9%p 어긋난다.** 정확히 맞지는 않았다. 이 크기가 −10.6 HOTA 를 만들 수는 없지만
"안 맞았다" 는 사실은 그대로 적는다. 정확히 맞춘 강건성 갈래는 아래 별도.

### 주 결과

| 갈래 | HOTA | DetA | **AssA** | LocA |
|---|---|---|---|---|
| **A (맨 IoU)** | **61.00** | 58.80 | **64.04** | 83.92 |
| W-size (= NWD) | 51.52 | 57.01 | 47.56 | 83.25 |
| W-DFL | 50.45 | 56.90 | 45.65 | 83.20 |

> **[1] W-DFL − W-size = −1.08 HOTA** → 판정폭 0.3 초과. **크기 소스가 낫다**
> **[2] W-DFL − A(IoU) = −10.56 HOTA** → **와서스타인 통로가 IoU 보다 훨씬 나쁘다**

### 시퀀스별 — [1] 은 가중 방식에 따라 부호가 흔들린다

| 시퀀스 | A(IoU) | W-DFL | W-size | [1] 차이 |
|---|---|---|---|---|
| MOT17-02 | 48.82 | 29.85 | 29.05 | **+0.79** |
| MOT17-04 | 68.77 | 62.81 | 64.85 | **−2.04** |
| MOT17-05 | 54.00 | 28.00 | 31.71 | **−3.72** |
| MOT17-09 | 62.97 | 42.69 | 41.98 | +0.71 |
| MOT17-10 | 51.85 | 36.65 | 36.59 | +0.06 |
| MOT17-11 | 59.10 | 37.89 | 37.69 | +0.20 |
| MOT17-13 | 51.65 | **56.83** | 56.17 | +0.66 |

- 검출 수 가중 (TrackEval 표준): **−1.077**
- 가중 없는 시퀀스 평균: **−0.476**
- DFL 이 이긴 시퀀스: **5/7**, 부호검정 양측 **p = 0.453**

**셋이 서로 다른 말을 한다.** 5/7 은 DFL 쪽이지만 유의하지 않고, 평균은 둘 다 음수이며,
큰 시퀀스 둘(04, 05)이 결과를 끌고 간다. → **[1] 은 "DFL 소스가 통로에서 낫다"를
지지하지 않는다.** 실험 1g 가 우도에서 본 5/7 (p=0.227) 이 **HOTA 로 옮겨가지 않았다.**

### 손상은 전부 연관에 있다

`AssA` 가 64.0 → 45.7~47.6 으로 무너지는데 `DetA` 는 58.8 → 56.9~57.0 으로 거의
안 움직인다. 검출은 갈래 간 **비트 단위로 같으므로** 이건 통제가 작동했다는 확인이자,
**비용함수 교체가 정확히 연관을 망가뜨렸다**는 뜻이다.

### 왜 망가지는가 — 수학 감사 (다)가 미리 말했다

`W² = ‖ε‖² + Bures`. 위치항이 **상자 크기로 정규화되지 않는다.** IoU 는 겹침 비율이라
큰 보행자의 10px 오차와 작은 보행자의 10px 오차를 자동으로 다르게 취급하는데,
`‖ε‖²` 은 **둘을 같은 무게로** 넣는다. MOT17 처럼 원근에 따라 상자 크기가 10배 이상
갈리는 장면에서 이건 치명적이다.

> 감사에서 **"와서스타인은 공분산 역설을 메커니즘을 없애서 푼다"** 고 적었다.
> **−10.56 이 그 대가다.** 역설을 피하려고 정규화를 통째로 버린 것이다.

> ## ⚠ 위 문단은 **틀렸다** — 실험 5b 가 반증했다 (2026-08-17 21:10)
>
> **정규화 부재가 원인이라고 썼는데, 정규화하면 더 나빠진다.**
> 크기 정규화 와서스타인은 **48.88** 로, 정규화 안 한 51.52 보다 **2.6 더 낮다.**
> 사전 선언 [3] = **−12.12** 로 "(A) 통로가 나쁘다" 구간에 들어간다.
>
> **원문을 지우지 않고 남긴다.** 이 프로젝트의 규칙이다. 근거는 「실험 5b」 절.

**예외가 하나 있다: MOT17-13 에서만 와서스타인이 IoU 를 이긴다 (56.83 vs 51.65).**
13 은 카메라가 크게 움직이는 시퀀스다. n=1 이라 주장으로 쓰지 않는다.

### MOT17-05 가 또 최악이다 — 네 번째다

| 어디서 | 무엇 |
|---|---|
| 실험 1 | χ² 이상치 |
| 1d imgsz 스윕 | 유일하게 부호가 뒤집힘 |
| 1g DFL 우도 | 유일한 패배 |
| **실험 5** | **[1] 최대 손실 (−3.72)** |

여전히 설명이 없다. 해상도(640×480)도 카메라 움직임도 letterbox 산술도 아니었다.
**이건 미해결로 남긴다.**

---

## 강건성 — 채택률을 **정확히** 맞추면 (2026-08-17 21:00)

함정 3 이 2.9%p 어긋난 것을 그냥 두지 않았다. `accept_rate.py --solve` 로
채택률을 기준선에 맞추는 `C` 를 이분법으로 찾아 다시 돌렸다.
**사전 선언한 보정 절차는 그대로 두고 별도 갈래로 붙인다.**

| 갈래 | C | 채택률 | HOTA |
|---|---|---|---|
| A (IoU) | — | **96.33%** | **61.00** |
| W-size (채택률 맞춤) | 234.68 | **96.42%** | **53.64** |
| W-DFL (채택률 맞춤) | 186.82 | **96.29%** | **53.04** |

| 종말점 | 중앙값 보정 (주 결과) | **채택률 맞춤** | 차이 |
|---|---|---|---|
| **[1]** W-DFL − W-size | −1.077 | **−0.597** | +0.48 |
| **[2]** W-DFL − A | −10.556 | **−7.963** | **+2.59** |

> **결론은 바뀌지 않고, 크기는 바뀐다.**
> [2] 의 −10.56 중 **약 2.6 은 임계값 효과였다.** 남는 −7.96 이 통로 효과다.
> [1] 은 −0.60 으로 줄지만 여전히 판정폭 0.3 을 넘는다 — **크기 소스가 낫다.**
>
> **주 결과를 이 값으로 갈아치우지 않는다.** 사전 선언한 절차는 중앙값 보정이었고,
> 이건 그 뒤에 붙인 강건성 확인이다. **둘 다 보고한다.**

---

## 실험 5b — 크기 정규화. **내 가설이 틀렸다** (2026-08-17 21:10)

사전 선언: `PREREG-norm.md` (**자료 보기 전 커밋** `c842aec`).

`W²` 의 위치항이 상자 크기로 정규화 안 되는 것이 −10.56 의 원인이라고 봤다.
쌍마다 `s_x=√(w_t·w_d)`, `s_y=√(h_t·h_d)` 로 좌표를 나눈 뒤 같은 `W²` 을 썼다
(아핀 사상이므로 **여전히 정당한 2-와서스타인**, `selftest.py [9]~[12]` 로 검산).

| 갈래 | HOTA | DetA | **AssA** | LocA |
|---|---|---|---|---|
| **A (맨 IoU)** | **61.00** | 58.80 | **64.04** | 83.92 |
| W-size (정규화 **안** 함) | 51.52 | 57.01 | 47.56 | 83.25 |
| **wn_size (정규화 함)** | **48.88** | 56.86 | **42.92** | 83.20 |
| wn_dfl | 47.31 | 56.47 | 40.52 | 82.99 |

> ### [3] `wn_size − A(IoU)` = **−12.12**
>
> 사전 선언한 세 구간 중 **`< −5.0` → (A) 통로가 나쁘다** 에 들어간다.
> **정규화는 통로를 고치기는커녕 2.6 더 망가뜨렸다.**
>
> **[4] 는 해석하지 않는다.** 사전 선언에 "[3] 이 (A) 면 [4] 를 해석하지 않는다"
> 고 적었다. (숫자는 −1.57 이지만 망가진 통로에서 잰 소스 비교라 뜻이 없다.)

### 왜 더 나빠졌나 — 관찰 하나 (사후, 주장 아님)

`s_x = √(w_t·w_d)` 는 **검출 상자 크기를 포함한다.** 중심 거리가 같을 때
`w_d` 가 크면 `s_x` 가 커져 **비용이 내려간다.** 즉 **트랙이 더 큰 검출과
짝지어 비용을 낮출 수 있다.** IoU 에는 이게 없다 — 상자를 키우면 합집합이
같이 커져 IoU 가 떨어진다. **정규화가 새로운 병리를 들여왔다.**

Bures 항이 반대로 움직여 일부 상쇄하지만(정규화된 `σ'_d` 는 `w_d` 에 비례해
커진다), AssA 가 47.56 → 42.92 로 더 무너진 것을 보면 상쇄가 부족하다.

**다른 정규화(대각선 길이, 트랙 쪽만, √area)를 더 시도하지 않는다.**
사전 선언에 "하나만 고른다 — 여러 개 재고 좋은 걸 고르면 그게 다중검정" 이라
적었다. **그 약속을 지킨다.**

### 이 반증이 실제로 하는 일

**실험 5 의 결론이 강해진다.** −10.56 의 가장 그럴듯한 대안 설명이 사라졌다.

| 후보 설명 | 검증 | 결과 |
|---|---|---|
| 임계값 효과 | 채택률 정확히 맞춤 | **2.6 만 설명한다.** −7.96 이 남는다 |
| 정규화 부재 | 실험 5b | **반증.** 정규화하면 오히려 −12.12 |
| 검정력 부족 | 관문 [0b] | **반증.** CV(Σt)=1.52, 임계의 30배 |
| 검출 차이 | 캐시 재생 | **원천 차단.** 갈래 간 비트 단위 동일 |

**네 대안이 모두 닫혔다.** 남는 것은 **"분포 유사도라는 의미론 자체가
MOT 연관에 안 맞는다"** 다.

---

## 상태

- **1단계 검출 캐싱: 완료** — 7시퀀스 2652 프레임, 63137 검출
- **2단계 비용함수 + 자체시험: 완료** — `wcost.py`, `selftest.py` **8/8 PASS**
  - 대각 단순화 == 일반형 (2.1e-14)
  - **공분산 역설 없음** 확인: Σ 를 키우면 비용이 53 → 164 로 **증가**한다.
    같은 조건에서 마할라노비스는 7.19 → 0.07 로 **감소**한다 (역설).
    `theory/divergence_channels.py` 의 [3] 을 비용함수 구현 수준에서 재확인
  - 규모 맞추기가 E[tr Σ] 를 정확히 맞춘다 (550.3 → 0.4977)
  - C 보정이 목표 중앙값을 6자리까지 맞춘다
- **3단계 트래커 재생: 배선 완료** — `replay.py`
  - 훅은 `get_dists` 하나. 2단계·GMC 는 손대지 않는다
  - `STrack` 상자 속성은 `.xyxy` 다 (`.tlbr` 은 UTrack 쪽 이름 — 한 번 틀렸다)
- **4단계 HOTA 평가: 배선 완료** — `evaluate.py`
  - **seqmap 적재 경로를 안 쓴다.** exp02 가 seqmap 헤더 누락으로 시퀀스를
    하나 조용히 잃었다. TrackEval 의 **지표 클래스만** 쓰고 적재는 직접 한다
  - 대신 **MOTChallenge 전처리를 소스 그대로 복제**했다: distractor
    (클래스 2·7·8·12) 매칭 제거 → `zero_marked`/pedestrian 필터 → id 재부여.
    distractor 제거를 빼먹는 재구현이 흔하고, 그러면 HOTA 가 몇 점씩 틀린다
- **판정: 완료** — 위 「결과」 절. **[1] −1.08, [2] −10.56. 둘 다 음수다.**

## 재현

```
python experiments/exp05_wasserstein/cache_detections.py
python experiments/exp05_wasserstein/selftest.py
python experiments/exp05_wasserstein/replay.py iou w_dfl w_size
python experiments/exp05_wasserstein/accept_rate.py iou w_dfl w_size
python experiments/exp05_wasserstein/evaluate.py iou w_dfl w_size
```
