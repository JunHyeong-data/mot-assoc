# CLAUDE.md

MOT 연관 비용에 검출 불확실성을 넣을 수 있는가를 통제된 실험으로 묻는 연구
저장소. **이 파일은 코드 안내이자, 이 프로젝트가 철회 16건을 대가로 배운
방법론 규범이다. 규범 쪽이 더 중요하다.**

---

## 절대 규칙 — 어기면 결과가 무효다

### 1. 사전 등록을 자료보다 **먼저 커밋**한다

새 실험은 `PREREG-*.md` 를 쓰고 **커밋한 뒤에** 자료를 만든다. 사전 등록에는
조건, 평가지표, 판정 기준, 사전 점검, 함정, **그리고 결과 시나리오별 결론**을 미리 적는다.
"어느 쪽이 나와도 그럴 줄 알았다" 를 못 하게 하는 것이 목적이다.

### 2. **사전 등록한 절차를 안 지킨 값으로는 판정도 철회도 하지 않는다**

**이게 철회 15번이고 이 저장소에서 가장 비싼 교훈이다.** 사전 등록은 "채택률
맞춤" 이었는데 "중앙값 보정" 값으로 반증을 선언했다. 25분 뒤 제대로 재니
**부호가 뒤집혔다** (−2.6 ↔ +2.4). 어긋남을 이미 알고 문서에 적어놨는데도
빨리 나온 쪽으로 결론을 냈다.

> 절차가 어긋난 것을 발견하면 **판정을 미룬다.** 어긋난 값으로 앞서 가지 않는다.

### 3. 같은 양을 **다른 경로로 한 번 더** 잰다

exp04 가 exp01 의 letterbox 버그를 잡았다. 두 실험이 같은 양을 다르게 말하면
둘 중 하나가 틀린 것이다. **실험이 겹치는 자리는 곱셈으로 대조하라.**
단위와 크기의 상식 검사(`E[w]=1010 px` 인 보행자는 없다)가 조용한 버그를 잡는다.

### 4. **철회한 것을 지우지 않는다**

틀린 결론은 취소선으로 남기고 왜 틀렸는지 적는다. `notes/progress.md` 의
철회 표가 이 프로젝트의 신뢰 근거다.

### 5. 가중과 비가중을 **둘 다** 보고한다

TrackEval 의 `combine_sequences` 는 검출 수 가중이라 큰 시퀀스가 지배한다.
가중 여부로 **부호가 뒤집히는 일이 실제로 두 번** 있었다 (exp05, exp06).
주 판정은 하나로 고정하되 둘 다 적는다.

### 6. **판정 기준은 설계 해상도 안에서 정한다** (2026-08-18 신설)

이 저장소는 판정 기준 0.3 HOTA 를 써 왔는데, `experiments/exp07_power` 가
**n=7 에서 검출 가능한 최소 효과는 3.31 HOTA** 임을 보였다. 0.3 은 애초에
분해할 수 없는 크기였다.

- HOTA 기반 주장의 분석 단위는 **장면**이고 지금 n=7 이다
- n=7 에서 부호검정이 유의해지는 유일한 길은 **만장일치(7/7, p=0.0156)** 다
- **"차이 없다" 는 동등성 주장**이다. 점추정이 판정 기준 안에 있는 것으로는 부족하고
  신뢰구간 전체가 들어가야 한다. n=7 에서 TOST 성공률은 0.000 이다

> **그러므로: 추적 수준 결과는 추론이 아니라 기술로 쓴다.**
> "이 7개 장면에서 이득이 관측되지 않았다" + **오라클 상한을 함께 보고**.
> 검출 수준 통계(단위=검출, n=수만)에는 이 제약이 없다.

---

## 저장소 구조

```
theory/        합성/증명. numpy+scipy 만, GPU 불필요
experiments/   실데이터. exp00~exp07
notes/         진행 기록, 방향 점검, 자체 심사, 논문 노트
data/          (git 제외) MOT17, 검출 캐시
external/      UTrack 클론 (벤더링된 TrackEval 을 여기서 쓴다)
```

**읽는 순서**: `notes/progress.md`(무엇이 확정·철회됐는가) →
`notes/direction.md`(선행연구와 기여) → `notes/self_review.md`(약점).

---

## 실험 배관 — 알아야 할 것

### 검출은 캐시하고 재생한다

`experiments/exp05_wasserstein/cache_detections.py` 가 npz 를 만들고(약 110분,
한 번만) 나머지는 그걸 재생한다. **조건을 바꿔도 검출이 비트 단위로 같다.**
exp05·exp06·exp07 이 같은 캐시를 쓴다.

```bash
python experiments/exp05_wasserstein/replay.py iou w_dfl w_size
python experiments/exp05_wasserstein/evaluate.py iou w_dfl w_size
python experiments/exp06_levers/grid.py     # 임계값 그리드 + 사전 점검
python experiments/exp06_levers/loso.py     # 적응 규칙 판정
python experiments/exp07_power/power.py     # 설계 계산
```

### HOTA 는 seqmap 을 쓰지 않는다

exp02 에서 **seqmap 헤더 누락으로 첫 시퀀스가 조용히 빠져** ΔHOTA 가
−0.43 → −0.62 로 바뀐 적이 있다. 그래서 `evaluate.py` 는 TrackEval 의
**지표 클래스만** 쓰고 적재는 직접 한다. 대신 **MOTChallenge 전처리를 소스
그대로 복제**해야 한다 — distractor 매칭 제거를 빼먹는 재구현이 흔하고
그러면 HOTA 가 몇 점씩 틀린다.

> **시퀀스가 조용히 빠지는 것을 항상 막아라.** 집계 함수는 빠진 시퀀스가
> 있으면 죽어야 한다. 조건마다 시퀀스 집합이 다르면 그 차이를 빼는 것 자체가
> 무의미하다.

### 트래커 한계 — 논문에 반드시 적을 것

우리가 쓰는 **ultralytics 8.4.46 은 ByteTrack 2단계가 죽어 있다.**
`fuse_score=True` 기본값과 2단계 하드코딩 임계값 0.5 가 충돌해
`cost ≥ 1−s > 0.75 > 0.5` 라 어떤 쌍도 통과 못 한다. 상류 PR #25034 가
2026-07-06 에 고쳤다(우리 발견이 아니다). **exp00·02·03·05·06 이 전부
2단계 비활성 트래커 위에서 돌았다.** 조건 간 비교는 유효하지만 "ByteTrack
기준선" 이라고 부르면 안 된다.

**얼마나 문제인가는 쟀다 (실험 8).** 2단계를 살리면 (2단계에서만 `fuse_score`
를 끈다 — 참조 구현과 같게) 채택이 0 → **782건**이 되고 기준선이
**−0.324 HOTA** 움직인다. 추적 수준 대조가 −4.33~−7.96 이므로 **한 자릿수
작다.** 한계로 적되 결론을 뒤집지는 않는다.
**단, 다른 조건을 고친 트래커에서 재실행한 적은 없다** — 그러려면 `C` 보정도
고친 트래커에서 다시 풀어야 한다(규칙 2).

---

## 코드 관례

- **한글 print 는 `ultralytics` 가 stdout 을 UTF-8 로 바꿔주기 때문에 된다.**
  ultralytics 를 import 안 하는 스크립트는 `sys.stdout.reconfigure(encoding="utf-8",
  errors="replace")` 를 **직접 써라.** 안 그러면 cp949 로 나가 깨진다.
  em-dash(—)나 화살표(→)는 어느 쪽이든 피하고 `->`, `<-` 를 쓴다
- **난수는 고정 씨앗으로.** `hash()` 를 씨앗으로 쓴 적이 있고 실행마다 다른
  세계가 됐다 (철회 10번). `np.random.default_rng(고정값)` 만 쓴다
- **임계값 같은 격자값은 표준 인코딩 함수 하나에서 나오게 한다.**
  폴더 이름과 JSON 키를 따로 만들면 반올림이 갈려 조용히 어긋난다
  (`experiments/exp06_levers/grid.py` 의 `code()`)
- 라이브러리를 가로챌 때는 **입력이 제자리에서 바뀌는지**와 **가로챈 뒤에도
  변환이 남아 있는지**를 확인한다. 둘 다 조용히 틀린다 (NMS in-place, letterbox)
- 판정 스크립트는 **사전 점검을 먼저** 둔다. 사전 점검이 실패하면 판정하지 말고 멈춘다.
  대조할 기록이 하나도 없으면 **통과시키지 말 것** — 그건 검사를 안 한 것이다

---

## 커밋

- 메시지는 한국어, **무엇을 왜 했고 무엇이 뒤집혔는지** 적는다
- 사전 등록은 자료보다 먼저, **별도 커밋으로**
- 결과를 본 뒤 코드를 고쳤으면 **그 사실을 주석과 커밋에 남긴다**
  (판정 로직을 안 건드렸다는 것도 함께)
- 끝에 `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`

---

## 데이터 라이선스

MOT17 은 **CC BY-NC-SA 3.0 — 비상업 연구만.** `data/` 는 git 에서 제외한다.
자세한 의무사항은 `notes/data_sources.md`.

### 7. **원고의 문장도 자료와 같은 검사를 받는다** (2026-08-18 신설)

철회 16번은 실험이 아니라 **원고를 쓰다가** 생겼다. 6.2 절에 이렇게 적었다:

> ~~*"비등방 형태를 쓰면 C 가 더 강해질 것이므로 우리 결론은 보수적이다."*~~

재니 반대였다 (실험 16: 분산비 0.973 대 비등방 가정 0.128, 비등방 구간 **0/7**).

> **"~할 것이므로 ~하다" 를 쓰는 순간 그건 대개 안 잰 것이다.**
> 원고 초고가 나오면 `가능성이 높다`, `것이므로`, `일 것`, `으로 보인다` 를
> 훑어서 **각각 잰 것인지 확인하라.** 잰 게 아니면 재거나 빼라.

같은 스캔에서 하나가 더 나왔다 — "UncertaintyTrack 의 이득은 기하 확장에서
온 것일 가능성이 높다". **그들은 2차 통과에 GIoU 로 쓰고 우리는 1단계 IoU 다.**
남의 결과를 반박하려면 **그 구조를 그대로 복제**해야 한다. 한계로 옮겼다.

### 8. **확장을 어느 쪽에 주는가** (2026-08-20 신설, 철회 17번)

게이팅 실험에서 상자를 키울 때 **검출만 키우면 개입의 방향이 뒤집힌다.**
트랙 상자가 커진 검출 안으로 들어가 교집합은 그대로인데 합집합만 커져서
**이미 잘 맞는 쌍의 IoU 가 오히려 떨어진다.** 확장이 문을 **여는** 게 아니라
**닫는** 개입이 된다.

`experiments/exp03_box_relaxation/box_relax.py:60` 이 이 함정을 **주석으로
적어 뒀는데**, 두 실험 뒤 `exp19_grid` 가 검출만 키워 같은 함정에 빠졌다.
채택률이 α 에 감소하는 것을 "새 발견" 으로 적을 뻔했다.

> **실험을 새로 짤 때 이전 실험의 환경변수 기본값을 확인하라.**
> exp03 은 `RELAX_APPLY=both` 가 기본이고 실제 실행도 `both` 였다.
> **통제 변수가 실험 사이에서 조용히 뒤집히는 것이 이 저장소의 반복되는 실패다**
> (letterbox 좌표, NMS in-place, 그리고 이것).

### 9. **가르는 실험은 두 예측을 표로 적고 시작한다** (2026-08-20 신설)

기제를 가르는 실험을 설계할 때 **사전 등록에 이 표를 반드시 넣는다:**

| | 이 측정에서 무엇을 예측하나 |
|---|---|
| 가설 (가) | … |
| 가설 (나) | … |

> **두 줄이 같으면 그것은 가르는 실험이 아니다.**

실제로 겪었다. "쌍 어긋남 대 두꺼운 꼬리" 를 가르겠다고 트랙을 **그 쌍의 검출 σ**
로 키웠는데, 그 설계는 **두 경로를 다 없앤다** — σ 가 커도 두 상자가 함께 부풀어
IoU 가 1 로 가므로 꼬리의 손해도 사라진다. **두 가설이 똑같이 "NMS ↑" 를
예측했고, 표로 적었으면 코드를 쓰기 전에 보였다.**

이건 규칙 7("안 잰 것을 적지 마라")과 다른 자리다. **재긴 쟀는데 잰 것이
질문에 답하지 않는 경우**이고, 검사기로는 못 잡는다. **설계 단계의 표 하나가
방어다.**
