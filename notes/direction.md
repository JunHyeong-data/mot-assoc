# 방향 점검 — 우리가 어디 서 있는가 (2026-08-17)

실험 1g 로 처음 긍정 결과가 나왔다. **그 전에 방향이 옳은지 문헌으로 확인했다.**
결론부터: **빈 칸은 실재한다. 다만 우리가 새롭다고 생각한 것 중 하나는 이미 있다.**

---

## 1. 선행연구 지도 — 소스 × 통로

불확실성 연구를 **어디서 얻는가(소스)** × **어디로 넣는가(통로)** 로 가르면 이렇다.

| 논문 | 소스 | 통로 | 결과 |
|---|---|---|---|
| **GFLv2** (CVPR'21) | **DFL 분포 통계 (top-K)** | 검출 품질 추정(LQE) — **MOT 아님** | +2.6 AP |
| **LG-Track** (2023, IEEE'24) | localization confidence — **YOLOX 라 DFL 아님** | **라우팅** (비용행렬 선택) | — |
| **UncertaintyTrack** (2024) | 검출기 공분산 | 칼만 R(−0.1) + **상자확장(+2.3)** | 이득은 게이팅 |
| **UTrack** (ECCV'24 W) | **NMS 후보 분산** | 칼만 R + IoU 모호성 | 우리 재현 **−0.62** |
| **NWD / LQTTrack** | **상자 크기** 가우시안 | **와서스타인** (IoU 대체) | — |

### 여기서 두 가지가 확정된다

**① 우리가 새롭다고 여긴 것 하나는 이미 있다.**
GFLv2 가 **"DFL 분포의 산포 ↔ 위치 품질"** 을 2020~21 에 이미 세웠다. 논문 표현:
분포가 뾰족하면 위치 품질이 높고 평평하면 낮다, 그 통계로 IoU 를 예측한다.
**그러므로 "DFL 분산이 위치 불확실성이다" 는 우리 기여가 될 수 없다.**

**② 그런데 빈 칸은 남아 있다.** 셋이 각자 한 발씩 걸쳤는데 아무도 다 가지 않았다.

- **GFLv2** 는 그 소스를 만들었지만 **검출 단계에서 멈췄다.** MOT 로 안 갔다
- **UTrack** 은 MOT 로 가져왔지만 **일부러 헤드를 안 건드렸다.** 논문이 명시한다 —
  *"성공적인 실시간 검출기를 수정하는 대신 추론 **이후에** 측정한다"* — 그래서
  NMS 후보 분산을 썼다. **우리 실험 1g 는 정확히 그들이 피한 자리다**
- **LG-Track** 은 위치 신뢰도를 MOT 에 넣었지만 **DFL 이 아니고**(YOLOX)
  **비용이 아니라 라우팅**이다
- **NWD** 는 와서스타인을 MOT 에 넣었지만 공분산이 **상자 크기**다 — 우리 실험의
  기준선 `C` 와 같다

> **빈 칸: DFL 분포 분산 → MOT 연관 비용에 공분산으로.**
> 그리고 우리에겐 그 칸을 채울 근거가 있다 — **DFL 이 크기모형을 6/7 이기고
> NMS 는 0/7 진다** (실험 1f·1g).

**중요한 부수 효과**: `C(상자크기 모형)` 은 우리가 만든 허수아비가 아니다.
**NWD/LQTTrack 이 실제로 배포한 방법이 바로 그것**이다. C 를 이긴다는 것은
통계적 허들이 아니라 **현행 방법을 이긴다**는 뜻이다.

---

## 2. 그래서 우리 기여는 무엇인가 — 정직하게

**기여가 아닌 것**
- ~~DFL 분산 = 위치 불확실성~~ → GFLv2 (CVPR'21)
- ~~와서스타인을 MOT 에 쓴다~~ → NWD/LQTTrack
- ~~불확실성을 MOT 에 넣는다~~ → UTrack, UncertaintyTrack, LG-Track

**기여인 것**

1. **통로 이론과 그 실증.** 행/열 분리성 + 임계값 조건으로 "어느 통로가 원리적으로
   가능한가" 를 가르고, **문헌의 성패가 그 예측과 맞는다**는 것 (칼만 R 은 둘 다
   손해, 이득은 전부 게이팅). 이건 아무도 안 했다
2. **아무도 안 돌린 통제군.** 확장량을 맞춘 K2 대조 — UncertaintyTrack 의 +2.3 이
   불확실성 때문인지 기하 때문인지 가르는 실험. **−3.81 로 기하 쪽이다**
3. **소스가 통로보다 중요하다는 실증.** 같은 통로·같은 판정으로 두 소스를 재니
   **성격이 정반대**다 (NMS 순위O 모형X / DFL 순위X 모형O).
   **문헌의 두 MOT 시도(UTrack의 NMS, UncertaintyTrack의 공분산)가 소스를 잘못
   골랐다**는 것이 여기서 나온다
4. **왜 실패하는지의 수치.** UncertaintyTrack 이 *"가우시안 가정과 안 맞는 듯"* 이라
   **추측**한 자리에 숫자 셋 — 눈금 24배, 꼬리비 12~213, t 로 고쳐도 0/7
5. **방법론.** 사전 선언 + 통제군 + **철회 10건**을 지우지 않고 남긴 기록

---

## 3. 논문을 어떻게 세울 것인가 — 세 선택지

### A. 음성 결과 + 방법론 논문 (**지금 당장 쓸 수 있다**)

> "검출 불확실성은 연관에 도달하지 못한다: 통제된 통로 분석"

기여 1·2·4·5 로 구성된다. **이미 다 있다.** UTrack 이 실린 UnCV 같은 워크숍이나
재현성 트랙이 자연스럽다.

- **강점**: 완결돼 있고, 정직하고, 재현 가능하다. 철회 10건이 약점이 아니라
  **신뢰의 근거**가 된다
- **약점**: "그래서 뭘 해야 하나" 가 없다

### B. DFL 소스 논문 (**아직 얇다**)

> "공짜 위치 불확실성: DFL 분포 분산을 MOT 연관에"

- **필요한 것**: p 를 굳히고(진행 중), 와서스타인 통로로 **HOTA 를 실제로 올려야** 한다
- **위험**: −0.096 nats 가 HOTA 로 옮겨간다는 보장이 없다. 실험 3 이 보였듯
  **기하 통로는 σ 를 벌한다.** 성능이 안 오르면 이 논문은 성립 안 한다

### C. **소스 × 통로 통제 연구** ← **권한다**

> "어느 불확실성을, 어디로? MOT 연관의 통제된 연구"

A 를 척추로 쓰고 **DFL 을 건설적 전환점**으로 놓는다. 주장은
"트래킹을 좋게 만들었다" 가 아니라 —

> **문헌의 두 시도는 통로가 아니라 소스에서 실패했다.**
> 같은 판정 절차로 두 소스를 재면 성격이 정반대이고,
> 아무도 안 쓴 소스(DFL)만 크기 기준선을 넘는다.

- **강점**: 음성 결과가 이미 강하므로 **DFL 이 HOTA 를 못 올려도 논문이 선다.**
  B 의 위험을 A 가 받쳐 준다
- **강점**: `C` 가 NWD 라는 배포된 방법이므로 기준선이 정당하다
- **필요한 것**: p 굳히기(진행 중) + 와서스타인 통로 실측 1회

---

## 4. 정직하게 봐야 할 위험 넷

1. **이론 신규성이 안 확인됐다.** "직사각에서의 조건부 불변성" 은 운용과학에서
   이미 알려져 있을 가능성이 높다 (더미 행/열로 정사각화하면 자연히 따라온다).
   **논문 쓰기 전에 Burkard 등 교재로 확인할 것.** 살아남는 건 (2) 비용 설계
   제약으로의 적용과 (3) 임계값이 그걸 깬다는 것이라고 본다
2. **표본이 작다.** MOT17 val_half 7시퀀스. fold 가 7개뿐이라 부호검정이
   6/7 이어도 p=0.0625 다. **MOT20·DanceTrack 을 넣어야 단단해진다**
3. **검출기가 한 계열이다.** 전부 ultralytics YOLOv8 계열. "검출기 무관" 주장을
   두 가중치로만 했다
4. **긍정 결과가 얇다.** −0.096 nats, p=0.0625. **이걸 세게 쓰면 안 된다.**
   지금 문서에 그렇게 적어 뒀고 유지할 것

---

## 5. 다음 세 걸음

1. **p 굳히기** (진행 중) — 저자 가중치 DFL 7 fold 추가. 합쳐 14 fold 면
   12~14/14 일 때 p ≈ 0.001~0.006 으로 결정적이 된다
2. **와서스타인 × DFL 실측 1회** — `theory/divergence_channels.py` 가 와서스타인만
   세 진단을 통과한다고 했고, 이제 넣을 소스가 있다. exp03 구조를 재사용해 HOTA 를 잰다
3. **할당 문제 교재 확인** — 이론 신규성 위험 1번. 논문 착수 전에

**순서를 바꾸지 말 것.** 1 이 실패하면(예: 저자 가중치에서 3/7) 2 를 할 이유가
없고, 그때는 선택지 A 로 간다.

---

## 참고한 것

- GFLv2, *Generalized Focal Loss V2* (CVPR 2021) — arXiv 2011.12885
- LG-Track (arXiv 2309.09765), Deep LG-Track (arXiv 2504.01457)
- UTrack (arXiv 2408.17098, ECCV'24 UnCV) — **"추론 이후에 측정한다"** 를 명시
- UncertaintyTrack (arXiv 2402.12303)
- NWD (tiny object detection), LQTTrack — 와서스타인을 **상자 크기** 가우시안으로
