__pycache__/
*.py[cod]
.venv/
venv/
.idea/
.vscode/

# 데이터 · 가중치 · 결과물 (용량)
data/
*.pt
*.pth
runs/
outputs/

# 개인 문서
*.docx
*.pptx
*.hwp

# 외부 저장소 (git 제외)
external/

# exp03 부트스트랩 셀 - make_bootstrap.py 가 그때그때 만든다.
# 소스를 박제하므로 커밋해 두면 옛 코드가 조용히 콜랩에 설치된다.
experiments/exp03_box_relaxation/bootstrap_cell.txt


# 그림 PNG — 원고는 PDF 만 쓴다. 어디서도 참조하지 않는데 추적 바이트의 38%
# 였다. 눈으로 보기 좋으니 만들기는 하되 추적하지 않는다.
figures/*.png
