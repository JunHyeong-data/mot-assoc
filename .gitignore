__pycache__/
*.py[cod]
.venv/
venv/
.idea/
.vscode/

# 데이터 · 가중치 · 결과물 (용량)
data/
*.pt
*.pth
runs/
outputs/

# 개인 문서
*.docx
*.pptx
*.hwp

# 외부 저장소 (git 제외)
external/

# exp03 부트스트랩 셀 - make_bootstrap.py 가 그때그때 만든다.
# 소스를 박제하므로 커밋해 두면 옛 코드가 조용히 콜랩에 설치된다.
experiments/exp03_box_relaxation/bootstrap_cell.txt


# 그림 PNG — 원고는 PDF 만 쓴다. 대부분은 추적하지 않는다.
# **단 README 가 쓰는 셋은 예외다** — GitHub 은 PDF 를 렌더링하지 않으므로
# 이 셋은 죽은 무게가 아니라 실제로 참조되는 자산이다.
figures/*.png
!figures/fig_ceiling.png
!figures/fig_grid.png
!figures/fig_reversal.png
